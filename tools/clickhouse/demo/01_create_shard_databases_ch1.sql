-- ============================================================
-- Run on ch-1 only
-- Node ch-1 hosts shard 01 (primary) and shard 03 (secondary)
-- ============================================================

CREATE DATABASE IF NOT EXISTS homelab_cluster_shard_01;
CREATE DATABASE IF NOT EXISTS homelab_cluster_shard_03;
