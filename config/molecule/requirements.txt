# Molecule testing framework for Ansible
molecule>=6.0.0
molecule-plugins[docker]>=23.0.0

# Testinfra for infrastructure testing
pytest-testinfra>=10.0.0
pytest>=8.0.0

# SSH connectivity
paramiko>=3.0.0

# For delegated driver with Proxmox
ansible-core>=2.15.0
