# config/roles/argocd_install/defaults/main.yml
---
# Version from argocd.yml
argocd_version: "v3.4.2"

# Namespace for installation
argocd_namespace: "argocd"

# Manifest URL (from argocd.yml)
argocd_install_url: "https://raw.githubusercontent.com/argoproj/argo-cd/v3.4.2/manifests/install.yaml"
