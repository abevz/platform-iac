# config/playbooks/setup_mailserver.yml
---
- name: Setup Mailserver (docker-mailserver)
  hosts: mailservers
  become: yes

  vars:
    addon_state: "present"
    mailserver_domain: "dev.example.com"
    mailserver_hostname: "mail.dev.example.com"

  roles:
    - role: mailserver_setup
