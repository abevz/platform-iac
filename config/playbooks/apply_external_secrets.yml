---
- name: Bootstrap Vault auth and sandbox ExternalSecret
  hosts: all
  become: no

  vars:
    vault_eso_admin_token: "{{ vault_eso_admin_token | default('') }}"

  roles:
    - role: external_secrets_vault_bootstrap
