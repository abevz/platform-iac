---
# playbooks/configure_timezone.yml

- name: Configure system timezone on all nodes
  hosts: all # 👈 Укажи, на каких хостах (all, kube_nodes, и т.д.)
  become: yes
  roles:
    - set_timezone
