# MinIO Backend Secrets - EXAMPLE FILE
# Copy this file to backend.sops.yml and fill with your real values
# Then encrypt with: sops -e -i backend.sops.yml

# MinIO S3 endpoint for Terraform state storage
MINIO_ENDPOINT: https://s3.minio.example.com

# MinIO root credentials
MINIO_ROOT_USER: your-minio-access-key
MINIO_ROOT_PASSWORD: your-minio-secret-key
