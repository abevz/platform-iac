# platform-iac .gitignore (Финальный)

# --- OpenTofu / Terraform ---
# Игнорируем локальные файлы состояния
*.tfstate
*.tfstate.backup

# Игнорируем плагины и кэш провайдеров
.terraform/
.terraform.lock.hcl

# --- КЭШ И АРТЕФАКТЫ ВЫПОЛНЕНИЯ (НОВОЕ) ---
# Каталог кэша, используемый iac-wrapper.sh
.cache/ 

# --- Ansible ---
# Игнорируем виртуальное окружение Python
.venv/

# Игнорируем файлы повторов Ansible
*.retry

# Игнорируем кэш инвентаря Ansible
.ansible_inventory_cache.json
# Временный JSON-инвентарь, если он не был очищен
iac_inventory_*.json 

# Игнорируем временные инвентари от iac-wrapper.sh (устаревшее .ini)
iac_inventory_*.ini 

# Игнорируем временные файлы SOPS, созданные оберткой (КРИТИЧНО!)
/tmp/iac_vars_*.json
/tmp/iac_tfvars_*.json

# --- Kubernetes / Kubeadm ---
# Кэшированный файл команды kubeadm join (fetch)
/tmp/kubeadm_join_command.sh

# --- SOPS (Расшифрованные файлы) ---
# ВАЖНО: Git должен хранить *только* зашифрованные файлы (*.sops.yml)
# Вы должны явно игнорировать пути к *расшифрованным* версиям.
config/secrets/certbot/dns-multi.ini

# --- Общие ---
# Временные файлы редакторов и бэкапы
*.bak
*.tmp
*.temp
*.swp
*~

# Временный каталог
tmp/
